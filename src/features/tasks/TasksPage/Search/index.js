import { Input } from "../Input";
import { Wrapper } from "./styled";

export const Search = () => {

    return (
        <Wrapper>
            <Input
                placeholder="Type here to find a task"
            />
        </Wrapper>
    );
};